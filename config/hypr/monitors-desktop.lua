-- Dual monitor — auto-detect resolution, side by side.
hl.monitor({ output = "", mode = "preferred", position = "0x0",  scale = "auto" })
hl.monitor({ output = "", mode = "preferred", position = "auto", scale = "auto" })
